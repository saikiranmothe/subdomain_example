<h1>Current User :  <%= current_user.email %></h1>
<hr>
<h4>User Organizations</h4>
<hr>
<div class="text-center">
	<% current_user.organizations.each do |org| %>
				<%= org.name %>
					<%= link_to root_url(:subdomain => org.domain_name), root_url(:subdomain => org.domain_name) %></p>
				<hr>
	<% end %>  
</div>